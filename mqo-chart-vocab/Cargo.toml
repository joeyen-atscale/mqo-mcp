[package]
name = "mqo-chart-vocab"
version = "0.1.0"
edition = "2021"
description = "Canonical chart-recommendation.v1 mark vocabulary and conformance utilities"
license = "MIT OR Apache-2.0"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
