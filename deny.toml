# deny.toml — supply-chain policy for autobuilder-scaffolded projects.
# Run: cargo deny check

[graph]
targets = []

[advisories]
version = 2
yanked = "deny"
unmaintained = "workspace"
ignore = []

[licenses]
version = 2
allow = [
  "MIT",
  "MIT-0",
  "Apache-2.0",
  "Apache-2.0 WITH LLVM-exception",
  "BSD-2-Clause",
  "BSD-3-Clause",
  "ISC",
  "Unicode-3.0",
  "CC0-1.0",
  "Zlib",
  "MPL-2.0",
  "CDLA-Permissive-2.0",
]
confidence-threshold = 0.8

[bans]
multiple-versions = "warn"
# Path-only workspace deps (sibling crates) are exempt — they have no
# semver range to specify and are not published crate references.
wildcards = "allow"
deny = [
  # Crates that have caused real-world soundness or supply-chain incidents,
  # or whose usage is a strong smell in any new project.
  { crate = "openssl", reason = "use rustls; avoid native OpenSSL dependency unless absolutely required" },
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
allow-git = []
